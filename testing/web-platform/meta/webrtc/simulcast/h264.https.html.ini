[h264.https.html]
  bug: 1709960
  expected:
    if (os == "linux") and not debug and not asan and not tsan: [TIMEOUT, OK]
    if (os == "linux") and debug: [TIMEOUT, OK]
    if os == "mac": OK
    if os == "android": TIMEOUT
    [OK, TIMEOUT]
  [H264 simulcast setup with two streams]
    expected:
      if (os == "linux") and not debug and not asan and not tsan: [TIMEOUT, PASS]
      if (os == "win") and debug and (processor == "x86_64"): [PASS, FAIL, TIMEOUT]
      if (os == "win") and debug and (processor == "x86"): [FAIL, PASS, TIMEOUT]
      if (os == "win") and not debug: [PASS, FAIL, TIMEOUT]
      if (os == "mac") and debug: [FAIL, PASS]
      if (os == "mac") and not debug: [PASS, FAIL]
      if (os == "linux") and debug: [TIMEOUT, PASS, FAIL]
      if os == "android": TIMEOUT
      [PASS, TIMEOUT]
